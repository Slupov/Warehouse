@model IEnumerable<Warehouse.Data.Models.ApplicationSettings>

@{
    ViewData["Title"] = "Application settings";
    ViewData["TitleDescription"] = "Here you can see a list of all application settings linked to your company.";
}

<p>
    <a asp-action="Create" class="btn btn-success">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Company)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PricesPrecisionUnits)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PricesIncludeVAT)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Company.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PricesPrecisionUnits)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PricesIncludeVAT)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
